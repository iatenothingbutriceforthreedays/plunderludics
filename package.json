{
  "name": "plunderludics-site",
  "version": "1.0.0",
  "scripts": {
    "build": "env NODE_ENV=production npx eleventy --pathprefix 'plunderludics' --input=src",
    "serve": "npx eleventy --serve --input=src --output=docs",
    "debug": "DEBUG=* npx eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0-canary.15"
  },
  "dependencies": {
    "@root/walk": "^1.1.0"
  }
}
